REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=replace-with-a-random-local-password
REDIS_TLS=false

RATE_LIMIT_MAX=10
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_FAILURE_MODE=closed

# Only set this when the app is actually behind these proxies.
# TRUSTED_PROXIES=loopback,10.0.0.0/8
